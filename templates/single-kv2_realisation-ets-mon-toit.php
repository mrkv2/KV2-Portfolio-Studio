<?php
/**
 * ETS Mon Toit single-realisation wrapper.
 *
 * Keeps the selected core/theme template intact, then injects the dedicated
 * chantier-video section inside the realization article.
 */

defined( 'ABSPATH' ) || exit;

KV2PS_ETS_Mon_Toit::render_wrapped_single_template();
