<?php
/**
 * Content is deliberately preserved on uninstall.
 * Delete realizations, attachment metadata and settings manually after a verified backup if required.
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;
